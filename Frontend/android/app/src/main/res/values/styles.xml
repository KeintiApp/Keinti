<resources>

    <!-- Base application theme.
         Theme.EdgeToEdge is provided by react-native-edge-to-edge and handles
         transparent system bars + Android 15 edge-to-edge rendering automatically,
         replacing the deprecated statusBarColor / navigationBarColor APIs. -->
    <style name="AppTheme" parent="Theme.EdgeToEdge">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

</resources>
