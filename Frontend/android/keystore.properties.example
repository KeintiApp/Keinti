# Copy this file to `keystore.properties` (same folder) and fill in real values.
# DO NOT commit `keystore.properties`.

# Path is relative to the `android/` folder.
storeFile=app/keinti-upload-key.keystore
storePassword=REPLACE_WITH_YOUR_STORE_PASSWORD
keyAlias=keinti-upload
keyPassword=REPLACE_WITH_YOUR_KEY_PASSWORD
